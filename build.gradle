plugins {
    id 'java'
    id 'jacoco'
    id "org.sonarqube" version "4.3.1.3277"
}

sonar {
    properties {
        property "sonar.projectKey", "bhos-qa_l4-branch-coverage-ferrridd"
        property "sonar.organization", "bhos-qa"
        property "sonar.host.url", "https://sonarcloud.io"
    }
}

group = 'org.example'
version = '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    testImplementation platform('org.junit:junit-bom:5.9.1')
    testImplementation 'org.junit.jupiter:junit-jupiter'
}
jacoco {
    toolVersion = "0.8.9"
}


jacocoTestReport{
    dependsOn test

    reports {
        xml.required=true
        html.required=true
    }
}

test {
    useJUnitPlatform()
    finalizedBy jacocoTestReport
}